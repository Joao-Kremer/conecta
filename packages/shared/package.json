{
  "name": "@conecta/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Zod schemas, types, constants, permissions, errors",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@conecta/eslint-config": "workspace:*",
    "@conecta/tsconfig": "workspace:*",
    "@types/jest": "^29.5.14",
    "@types/node": "^20.17.10",
    "eslint": "^9.17.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.2"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "rootDir": "src",
    "testRegex": ".*\\.spec\\.ts$"
  }
}
