auto-install-peers=true
strict-peer-dependencies=false
# engine-strict desligado: algumas transitive deps (ex: eslint-visitor-keys)
# rejeitam Node 23 mesmo quando funcionam. CI roda Node 20 (LTS) via .nvmrc.
engine-strict=false
