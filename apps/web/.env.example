# Internal URL of the NestJS API (server-side only, never exposed to browser)
API_URL=http://localhost:3000

# Sentry (optional — leave empty to disable error reporting)
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_DSN=
